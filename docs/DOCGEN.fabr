# Minimal project staged as the docs generator's input: it loads the standard
# vocabulary (core is always present; the js plugin adds js_package et al) so
# `fabr list-targetdefs --json`, run against it, documents exactly what a
# fabr + @fabr-build/js user has. See PROJECT.fabr's `reference_docs` target.
plugin @fabr-build/js;
